{
  "name": "blockaccess",
  "version": "1.0.0",
  "description": "A React component and hook to control content access based on user geolocation and VPN status.",
  "main": "dist/index.js",
  "module": "dist/index.js",
  "files": [
    "dist",
    "src",
    "README.md",
    "LICENSE"
  ],
  "scripts": {
    "build": "rm -rf dist && NODE_ENV=production babel src --out-dir dist --copy-files"
  },
  "keywords": [
    "react",
    "access-control",
    "geolocation",
    "security",
    "vpn-detection",
    "geo-blocking"
  ],
  "author": "Your Name",
  "license": "MIT",
  "peerDependencies": {
    "react": ">=16.8.0",
    "react-dom": ">=16.8.0"
  },
  "devDependencies": {
    "@babel/cli": "^7.23.0",
    "@babel/core": "^7.23.2",
    "@babel/preset-env": "^7.23.2",
    "@babel/preset-react": "^7.22.15"
  },
  "dependencies": {
    "blockaccess": "github:yasakei/blockAccess#react"
  }
}
